<%= form_with(model: tess, local: true) do |form| %>
  <% if tess.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tess.errors.count, "error") %> prohibited this tess from being saved:</h2>

      <ul>
      <% tess.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :id %>
    <%= form.number_field :id, id: :tess_id %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.number_field :value, id: :tess_value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
